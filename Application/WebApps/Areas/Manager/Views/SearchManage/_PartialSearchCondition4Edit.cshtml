@using BussinessFacade.ModuleMemoryData
@using ObjectInfos;
@model SearchObject_Detail_Info
@{
    decimal _IdSearch = Model.ID;
    decimal _IsFirst = Model.IS_FIRST;

    List<AllCodeInfo> _lstObject = WebApps.CommonFunction.AppsCommon.AllCode_GetBy_CdTypeCdName("SEARCH_OBJECT", "OBJECT");
    List<AllCodeInfo> _ListSearchType = WebApps.CommonFunction.AppsCommon.AllCode_GetBy_CdTypeCdName("SEARCH_OBJECT", "SEARCHTYPE");

}
<div id='@("div_condition_" + _IdSearch)' class="div-search-ad">
    <input type="hidden" value="@_IdSearch" class="txtIdSeachClass" />
    @if (_IsFirst == 1)
    {
        <div class="div-checkbox" style="border: none;">
            <img src="~/Content/icons/condition.png" style="margin: 10px 5px 0px 5px;width: 13px;" />
        </div>
    }
    else
    {
        <div class="div-checkbox" style="border: none;">
            <img onclick="funcdeletecondition('@_IdSearch')" src="~/Content/icons/delete_file.png" style="margin: 10px 5px 0px 5px" />
        </div>
    }

    @*<div class="div-search-title">
        <div> Đối tượng <i class="redspan">(*)</i> </div>
    </div>
    <div class="div-search-content">
        <div>
            <select class="search_object" id="@("_cbosearch_object_" + _IdSearch)" style="width:150px">
                @foreach (AllCodeInfo item in _lstObject)
                {
                    if (item.CdVal == Model.SEARCH_OBJECT.ToString())
                    {
                        <option value="@item.CdVal" selected="selected">
                            @item.Content
                        </option>
                    }
                    else
                    {
                        <option value="@item.CdVal">
                            @item.Content
                        </option>
                    }
                }
            </select>
        </div>
    </div>*@

    <div class="div-search-title">
        <div> Loại <i class="redspan">(*)</i> </div>
    </div>
    <div class="div-search-content">
        <div>
            <select class="search_type" id="@("_cbosearch_type_" + _IdSearch)">
                @foreach (AllCodeInfo item in _ListSearchType)
                {
                    if (item.CdVal == Model.SEARCH_TYPE.ToString())
                    {
                        <option value="@item.CdVal" selected="selected">
                            @item.Content
                        </option>
                    }
                    else
                    {
                        <option value="@item.CdVal">
                            @item.Content
                        </option>
                    }
                }
            </select>
        </div>
    </div>
</div>