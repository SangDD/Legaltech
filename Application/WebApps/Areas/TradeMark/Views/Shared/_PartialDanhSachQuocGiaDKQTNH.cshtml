@using BussinessFacade.ModuleMemoryData
@model string
@{
    string _national01 = Model + "_cboNational01";
    string _national02 = Model + "_cboNational02";
    string _national03 = Model + "_cboNational03";
    string _national04 = Model + "_cboNational04";
    string _national05 = Model + "_cboNational05";
    string _national06 = Model + "_cboNational06";
    string _national07 = Model + "_cboNational07";
    string _national08 = Model + "_cboNational08";

 }
<div class="div-search-ad">
    <div class="div-search-content">
        <div style="width:320px">
            <select id="@_national01" class="borderleft4control">
                <option value="">Chọn quốc gia</option>
                @foreach (var item in MemoryData.c_lst_Country)
                {
                     
                        <option value="@item.Country_Id">@item.Name</option>
                }
            </select>
        </div>
    </div>
    <div class="div-search-content">
        <div style="width:320px">
            <select id="@_national02">
               <option value="">Chọn quốc gia</option>             
                @foreach (var item in MemoryData.c_lst_Country)
                {

                    <option value="@item.Country_Id">@item.Name</option>
                }
            </select>
        </div>
    </div>

    <div class="div-search-content">
        <div style="width:320px">
            <select id="@_national03">
                <option value="">Chọn quốc gia</option>
                @foreach (var item in MemoryData.c_lst_Country)
                {

                    <option value="@item.Country_Id">@item.Name</option>
                }
            </select>
        </div>
    </div>

    <div class="div-search-content">
        <div>
            <select id="@_national04">
                <option value="">Chọn quốc gia</option>
                @foreach (var item in MemoryData.c_lst_Country)
                {

                    <option value="@item.Country_Id">@item.Name</option>
                }
            </select>
        </div>
    </div>
</div>
 <div class="div-search-ad">
    <div class="div-search-content">
        <div style="width:320px">
            <select id="@_national05" class="borderleft4control">
                <option value="">Chọn quốc gia</option>
                @foreach (var item in MemoryData.c_lst_Country)
                {

                    <option value="@item.Country_Id">@item.Name</option>
                }
            </select>
        </div>
    </div>
    <div class="div-search-content">
        <div style="width:320px">
            <select id="@_national06">
                <option value="">Chọn quốc gia</option>
                @foreach (var item in MemoryData.c_lst_Country)
                {

                    <option value="@item.Country_Id">@item.Name</option>
                }
            </select>
        </div>
    </div>

    <div class="div-search-content">
        <div style="width:320px">
            <select id="@_national07">
                <option value="">Chọn quốc gia</option>
                @foreach (var item in MemoryData.c_lst_Country)
                {

                    <option value="@item.Country_Id">@item.Name</option>
                }
            </select>
        </div>
    </div>

    <div class="div-search-content">
        <div>
            <select id="@_national08">
                <option value="">Chọn quốc gia</option>
                @foreach (var item in MemoryData.c_lst_Country)
                {

                    <option value="@item.Country_Id">@item.Name</option>
                }
            </select>
        </div>
    </div>
</div>

<script>
    $('#' + '@_national01').multipleSelect({
        filter: true,
        noMatchesFound: "Không tìm thấy dữ liệu",
        single: true,
        placeholder: 'Tìm kiếm quốc gia'
    });
    $('#' + '@_national02').multipleSelect({
        filter: true,
        noMatchesFound: "Không tìm thấy dữ liệu",
        single: true,
        placeholder: 'Tìm kiếm quốc gia'
    });
    $('#' + '@_national03').multipleSelect({
        filter: true,
        noMatchesFound: "Không tìm thấy dữ liệu",
        single: true,
        placeholder: 'Tìm kiếm quốc gia'
    });
    $('#' + '@_national04').multipleSelect({
        filter: true,
        noMatchesFound: "Không tìm thấy dữ liệu",
        single: true,
        placeholder: 'Tìm kiếm quốc gia'
    });
    $('#' + '@_national05').multipleSelect({
        filter: true,
        noMatchesFound: "Không tìm thấy dữ liệu",
        single: true,
        placeholder: 'Tìm kiếm quốc gia'
    });
    $('#' + '@_national06').multipleSelect({
        filter: true,
        noMatchesFound: "Không tìm thấy dữ liệu",
        single: true,
        placeholder: 'Tìm kiếm quốc gia'
    });
    $('#' + '@_national07').multipleSelect({
        filter: true,
        noMatchesFound: "Không tìm thấy dữ liệu",
        single: true,
        placeholder: 'Tìm kiếm quốc gia'
    });
    $('#' + '@_national08').multipleSelect({
        filter: true,
        noMatchesFound: "Không tìm thấy dữ liệu",
        single: true,
        placeholder: 'Tìm kiếm quốc gia'
    });
</script>